-- =============================================================
-- Migration 034: тумблер ingest-job для модуля «Аукционы»
--                (этап 8/9 слияния, 2026-05-08).
--
-- Что делает: добавляет в settings ключ `auctions_ingest_enabled`
-- с дефолтом 'true'. Это рантайм-тумблер, который читает APScheduler-
-- задача `auctions_ingest` в portal/scheduler.py: если значение !=
-- 'true' (нечувствительно к регистру), плановый запуск пропускается.
-- На UI-страницу настроек (Этап 9) это позволит выключать ingest без
-- передеплоя — по той же логике, что и `auto_price_loads.enabled`.
--
-- Идемпотентно: ON CONFLICT (key) DO NOTHING. Повторный прогон не
-- перезаписывает значение, если админ уже выставил его руками.
-- =============================================================

INSERT INTO settings (key, value)
VALUES ('auctions_ingest_enabled', 'true')
ON CONFLICT (key) DO NOTHING;
