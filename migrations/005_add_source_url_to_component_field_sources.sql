-- =============================================================
-- КВАДРО-ТЕХ: миграция 005
-- Добавляет колонку source_url в component_field_sources.
--
-- Для источников 'claude_code' и 'ai' хранит URL официальной
-- страницы производителя, откуда взято значение. Для старых
-- источников ('regex', 'derived', 'manual') колонка остаётся NULL.
--
-- Требование этапа 2.5Б: любое значение от Claude Code принимается
-- только если указан URL оф. сайта. URL нужен для аудита и
-- повторной проверки спорных значений.
-- =============================================================

ALTER TABLE component_field_sources
    ADD COLUMN source_url VARCHAR(500);
