-- =============================================================
-- КВАДРО-ТЕХ: миграция 012 — поле «Контактное лицо» у поставщика (этап 9А.2)
--
-- Что меняет:
--   Добавляет contact_person в suppliers — имя ответственного у поставщика,
--   которое менеджер пишет в шапке письма «Здравствуйте, Иван!».
--
-- Существующие поля suppliers:
--   - email (миграция 011) — основной адрес для запроса цен
--   - contact_email, contact_phone (с миграции 001/009) — оставлены, не трогаем
--   - is_active (с миграции 001) — флаг активности, теперь редактируется в UI
--
-- Идемпотентно: ALTER TABLE ... ADD COLUMN IF NOT EXISTS.
-- =============================================================

ALTER TABLE suppliers
    ADD COLUMN IF NOT EXISTS contact_person VARCHAR(255);
