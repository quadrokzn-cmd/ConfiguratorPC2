__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Переменные окружения — никогда не коммитим
.env
.env.local
.env.*.local
.env.local.preprod*

.idea/
.vscode/
.DS_Store
Thumbs.db
*.log

# pytest
.pytest_cache/

# Служебные файлы Claude Code
.claude/

# Бизнес-данные (прайс-листы поставщиков и т.п.)
data/

# Дампы HTML для визуальных тестов (VISUAL_DUMP=1)
visual_samples/

# Архив stale pending-ключей (этап 2.5б) — локальная резервная копия,
# перенесена из подкорзины pending/ после regex-этапа.
enrichment/stale_pending_20260424/

# Pending-batch'и AI-обогащения (этап 11.6.2.4.1a) — runtime-артефакт,
# выгружается с прод-БД через scripts/enrich_export_prod.py непосредственно
# перед прогоном AI-чатов. В репо не нужен. Старые легаси batch'и под
# pending/ остаются tracked — gitignore их из истории не убирает.
enrichment/pending/

# Дизайн-референсы для этапа 9 (редизайн UI)
design_references/

# Frontend pipeline (этап 9А.1) — собранный CSS в static/dist/ коммитим
# (см. docs/design-decisions.md, решение №3), зависимости — нет.
node_modules/

# Логи scheduler'а (этап 9А.2.3): курсы ЦБ, фоновые задачи.
logs/

# Дампы БД (этап 10.3) — никогда не коммитим, содержат боевые данные
db_dumps/
*.dump

# Артефакты Playwright MCP (логи и снимки страниц при ручной проверке UI).
.playwright-mcp/

# MCP-конфиг (локальные креды/настройки серверов).
.mcp.json

# Локальные отчёты скриптов (диагностика, аудит, реклассификация).
# Содержат raw_name товаров — в репо не нужны.
scripts/reports/

# Диагностические артефакты разведки Resurs Media SOAP API (этап 12.4-РМ-0).
# Содержат raw цены, ассортимент, креды. В репо не нужны.
scripts/_diag_resurs_media_*

# Все диагностические скрипты-разведки (этапы 12.4 и слияния QT↔C-PC2).
scripts/_diag_*
scripts_diag_*

# Тестовые reference-прайсы для smoke (содержат raw цены поставщиков).
reference_prices/

# Pending/done batches AI-обогащения для аукционного модуля.
enrichment/auctions/pending/
enrichment/auctions/done/

# Seed-дамп для Railway pre-prod (этап 9d.1) — генерируется
# scripts/preprod_seed_dump.sh из локальной БД kvadro_tech
# непосредственно перед заливом в Railway-Postgres pre-prod.
# Содержит boevye prices поставщиков — в репо не нужен.
scripts/preprod_seed.sql
