# 2026-05-13 — Мониторинг первого боевого тика Resurs Media (07:40 МСК) — НЕ СОСТОЯЛСЯ

## 1. Задача

Оркестратор поставил: «убедиться, что первый prod-тик автозагрузки Resurs
Media в 2026-05-13 07:40 МСК отработал успешно». Контекст: 2026-05-12
собственник получил боевой SOAP API от РМ, подключил Variables на Railway
prod, прогнал `scripts/resurs_media_bootstrap_catalog.py` (Фаза C — 70 501
позиция в `resurs_media_catalog`). Сегодня — день первого scheduled-тика.

Скриншот собственника с `/databases/auto-price-uploads` (на самом деле
дашборд-виджет «свежесть прайсов» на главной) показывал «Ресурс Медиа:
Старый, 30.04.2026 17:09» — что нужно было либо подтвердить как баг UI,
либо объяснить.

## 2. Как решал

Worktree `feature/resurs-media-monitor` от `origin/master`.

Прочитал runner/fetcher/scheduler/UI-роутер, чтобы понять контракт:
- `portal/scheduler.py::_make_auto_price_job` → `_is_supplier_enabled(slug)`
  читает `auto_price_loads.enabled` для конкретного slug. Если FALSE —
  log INFO «тумблер OFF, пропуск» и тихий выход. **Запись в
  `auto_price_load_runs` НЕ создаётся** (это полезно: тумблер OFF = «как
  будто cron не существует»).
- `portal/services/dashboard.py::_get_suppliers_freshness` → читает
  `MAX(uploaded_at) FROM price_uploads WHERE status IN ('success','partial')`
  по каждому имени поставщика. Это и есть источник «30.04.2026 17:09» —
  он не связан с auto_price_loads напрямую.

Подключение к prod-БД через `DATABASE_PUBLIC_URL` из
`.env.local.prod.v1`, чтение через `dotenv_values()` без печати DSN в
stdout (memory `feedback_env_grep_content`). Скрипт
`scripts/_diag_resurs_media_first_tick.py` — одноразовая диагностика,
не закоммичен.

Результаты (см. план, мини-этап 2026-05-13 Фаза D, детали):

1. `auto_price_loads.resurs_media.enabled=FALSE`, last_success_at=NULL.
2. `auto_price_load_runs` за 2026-05-12..2026-05-13 для resurs_media —
   **0 строк**.
3. Все run'ы resurs_media за всё время — 2 строки от 2026-05-08
   (manual experiments перед получением боевого API).
4. `audit_log` за весь май с упоминанием `resurs_media` — **0 строк**.
5. `resurs_media_catalog` — 70 501 row (Фаза C прошла корректно
   2026-05-12 19:05-19:07 МСК).
6. `resurs_media_notifications` — 0 row.
7. `price_uploads` для supplier_id=8 — 2 ручных XLSX-загрузки от
   30.04.2026 (т.е. ровно за 13 дней до сегодня).

## 3. Решил ли

**Частично.** Диагноз поставлен однозначно: первого боевого тика
2026-05-13 07:40 МСК **не было**, потому что `auto_price_loads.enabled
=FALSE`. Запись в журнал не появляется — cron-job делает
`_is_supplier_enabled('resurs_media') → False` и тихо возвращается. Это
**сценарий A** из категорий, заданных оркестратором: «Cron disabled в
settings — не включай сам, это бизнес-решение».

Сам фиксить тут нечего: код корректный, scheduler корректный (если бы
тумблер был ON — тик прошёл бы). Действие — на стороне собственника:
один клик в UI `/admin/auto-price-loads` для «Ресурс Медиа».

## 4. Эффективно ли решение, что можно было лучше

**Эффективно** (≈45 минут от worktree до диагноза):
- Один Python-скрипт, 7 SQL-запросов, одна итерация на правке столбцов
  схемы (`source_filename` → `source` → `filename`; `fetched_at`
  → `synced_at`).
- Никаких изменений на prod, никаких сетевых запросов к SOAP, никакого
  трогания секретов.
- Безопасность секретов соблюдена: `dotenv_values()` без печати, regex
  redact для подозрительных подстрок в error_message.

**Чего могло быть лучше:**

1. **Предусмотреть seed-state.** При seed-данных миграции `enabled=FALSE`
   для всех 6 поставщиков (резонно — пока fetcher не подключён, не
   надо его звать). Но семантика «вот fetcher подключён, креды
   заведены, bootstrap-каталог построен → почему тогда `enabled` всё ещё
   FALSE» нигде не подсвечена. На UI `/admin/auto-price-loads`
   у строки RM на скрине бейдж «Канал не подключён» — это, видимо,
   потому что `enabled=FALSE`, а не потому что fetcher отсутствует
   (`register_fetcher` его зарегистрировал). UI-формулировка
   «Канал не подключён» вводит в заблуждение, когда `enabled=FALSE`
   при существующем fetcher'е.
   
   **Идея на будущее (не делал, не входит в скоуп):** разделить
   состояния в UI — «канал не подключён» (нет fetcher'а) vs
   «канал выключен» (есть fetcher, но enabled=FALSE) — это два разных
   бейджа.

2. **Cron мог бы оставлять след в `auto_price_load_runs`** со
   status='disabled', чтобы факт «scheduler пытался, но тумблер OFF»
   был виден в журнале. Сейчас он `logger.info(...)` уходит в
   Railway Deploy Logs (root WARNING обрезает — memory
   `reference_railway_deploy_logs`), и понять «cron сработал и нашёл
   OFF» vs «cron не запустился» можно только через `print(..., flush=True)`
   или через посекундный анализ Railway-логов. **Не делал**: смена
   контракта (что считать «попыткой запуска») должна быть отдельным
   архитектурным решением, не побочным эффектом мониторинга.

3. **Сделал бы UI-предупреждение «канал подключён, тумблер OFF».** Сейчас
   ничто на UI не подсказывает, что забыли включить — RM просто стоит
   как «выключенный», но различия с «не подключённым» нет. Не входит в
   текущий мини-этап.

4. **Action item: после включения тумблера — обязательно проверить
   первый боевой тик руками** (UI «Запустить сейчас»). Если оставить до
   scheduled 07:40 МСК завтра — а там сюрприз (rate-limit, auth-ошибка
   с боевыми кредами, ещё что-то) — потеряем сутки. Ручной тик сегодня
   покажет ответ за минуту.

## 5. Как было и как стало

**Было:**
- `auto_price_loads.resurs_media.enabled=FALSE` (seed-состояние
  с 2026-05-08, никогда не меняли).
- В журнале — пустота. На дашборде «свежесть прайсов» — последняя
  ручная XLSX-загрузка от 30.04.2026.
- Загадка «отработал ли первый prod-тик» без явного ответа.

**Стало:**
- Однозначный диагноз: тика не было, потому что тумблер OFF.
- В плане `plans/2026-04-23-platforma-i-aukciony.md` зафиксирован
  мини-этап 2026-05-13 Фаза D с точными SQL-данными и action item для
  собственника.
- Никаких изменений на prod: код, БД, секреты — без правок.

**Что осталось собственнику** (промт оркестратора прямо просил
AskUserQuestion в сценарии A; здесь зафиксировано в рефлексии для
оркестратор-чата — пусть он решит, спрашивать собственника
интерактивно или просто скинуть как информацию):

1. `/admin/auto-price-loads` → тумблер ON для «Ресурс Медиа».
2. (Опционально) сразу же кнопка «Запустить сейчас» — проверить fetcher
   с боевыми кредами.
3. Завтра утром мониторинг scheduled-тика 2026-05-14 07:40 МСК
   повторить (новый чат).

---

## ВТОРОЙ МИНИ-ЭТАП 2026-05-13: data-fix `_CATEGORY_GROUP_MAP`

### 1. Задача (продолжение)

Собственник выполнил оба шага: тумблер ON + «Запустить сейчас». Manual-
тик 2026-05-13 09:45 МСК отработал, но получил **`status='no_new_data'`**
с ошибкой «Resurs Media: GetPrices вернул пустой Material_Tab по всем
11 group_id (warehouse=00011)».

Та же ошибка, которая была на test-стенде 2026-05-08, но теперь — на
prod. Раз bootstrap-каталог собрал 70 501 row через GetMaterialData
(значит, боевой WSDL и creds работают), то проблема не в auth и не в
endpoint'е — а в **фильтре group_id**. Гипотеза: маппинг
`_CATEGORY_GROUP_MAP` (test-стенд) не совпадает с реальной иерархией
групп prod-каталога.

### 2. Как решал

Diagnostics через 4 одноразовых скрипта (не коммитятся):

1. `_diag_rm_groups.py` — сверка expected ↔ prod: для каждого из 11
   expected group_id'ов выполнить `SELECT COUNT(*) FROM
   resurs_media_catalog WHERE material_group = :g`. Также top-30
   prod-групп для понимания иерархии.
2. `_diag_rm_components.py` — поиск реальных компонентов ПК по
   keyword'ам и vendor (Intel/AMD/ASUS/...) в material_text.
3. `_diag_rm_components2.py` — углублённый поиск SSD/корпусов/
   внутренних HDD через бренды (Samsung/Kingston/Crucial/Aerocool/
   Cooler Master/...).
4. `_diag_rm_groups_purity.py` — проверка «чистоты» каждой кандидатной
   группы: top-5 vendor и 5 случайных наименований, чтобы убедиться,
   что в группе ИМЕННО ожидаемая категория (без смеси).

Открытия:

- **Все 11 expected group_id'ов — rows=0 в prod-каталоге.** Маппинг с
  test-стенда абсолютно несовместим с prod.
- **prod-каталог РМ** в первую очередь — расходники для оргтехники
  (топ-группы Z100521 n=8065 «Платы процессора изображений XEROX
  Phaser», Z100484 n=8012 «Чистящий вал. RICOH», Z100553 n=3506
  «Ролик Konica-Minolta» и т.п.) плюс готовые ноутбуки (Z100226/
  Z100240/Z100243 n≈4700). Но компоненты ПК тоже есть — отдельными
  узкими группами.
- **Найденные prod-группы для каждой нашей категории:**
  - psu → `Z100173` (~426 ATX-БП)
  - cooler → `Z100198` (~547)
  - gpu → `Z100175`/`Z100176`/`Z100177`/`Z100178` (~37, разбито по
    ASUS/GIGABYTE/MSI/PALIT)
  - storage SSD → `Z100160`-`Z100172` (12 групп, ~533)
  - storage HDD → `Z100109`-`Z100118` (5 групп, ~114)
  - motherboard → `Z100183`/`Z100184`/`Z100185` (~22)
  - ram → `Z100189` (DIMM, 136), `Z100195` (SO-DIMM, 63), `Z100193`
    (ECC Reg, 41), `Z100891` (DDR5, 84). Всего ~324 модуля.
  - case → `Z100180` (~386 корпусов)
  - cpu → `Z100197` (~57 OEM Intel/AMD)
- **Группа `Z100808` намеренно исключена** — в инспекции выяснилось, что
  там RAM-модули Synology вперемешку с сетевухами/направляющими/SATA-
  дисками. Если бы зачислили её в `ram`, в `supplier_prices` поехали
  бы сетевые карты под категорией `ram`. 61 row, не стоит риска.

Применил data-fix:

- `portal/services/configurator/auto_price/fetchers/resurs_media.py` —
  `_CATEGORY_GROUP_MAP` переписан с 11 test-group'ов на 22 prod-
  group'а. Docstring карты переписан с указанием источника, истории
  смены и обоснования выбора каждой группы.
- `tests/test_auto_price/test_resurs_media_fetcher.py` — 14 тестов
  адаптированы. Hardcoded test-group'ы (`Z431`, `Z999-10110`,
  `Z999-10006`, `Z383/Z897/Z373`) заменены на prod-эквиваленты
  (`Z100189`, `Z100197`, `Z100183`, `Z100109/Z100167/Z100160`).
  Счётчик `len(sent_groups) == 11` заменён на динамический
  `== sum(len(g) for g in _CATEGORY_GROUP_MAP.values())` — будущая
  правка маппинга не сломает тест.
- Тест `test_psu_dual_group_codes` (psu из 2 групп — на prod psu в
  одной группе, тест бессмыслен) переделан в
  `test_gpu_aggregates_four_vendor_groups` (gpu из 4 групп — то же
  по контракту «несколько group_id → одна our_category», но
  актуально для prod).
- `scripts/resurs_media_smoke.py` — константа `TEN_GROUPS` обновлена
  на prod-группы. Имя сохранено для совместимости с историей чек-
  листа Волкова.

Tests: `pytest tests/test_auto_price/test_resurs_media_fetcher.py` —
14 passed (контракт не сломан, новые prod-ID работают). Полный pytest
baseline после слияния — см. цифру в коммите.

### 3. Решил ли

**Да.** Маппинг под prod-стенд. Manual-тик после деплоя должен пройти:

- GetPrices вернёт ~1856 row (psu 426 + cooler 547 + gpu 37 + storage
  647 + motherboard 22 + ram 324 + case 386 + cpu 57; грубая оценка
  по resurs_media_catalog).
- GetMaterialData по дельте: catalog заполнен 70 501 row через
  bootstrap БЕЗ фильтра, так что все позиции из этих 22 group_id уже
  в catalog'е. Дельта вернёт 0 новых, может быть ~30-дневный stale —
  если позиции синкались 2026-05-12, то они НЕ stale (< 30 дней
  назад), GetMaterialData почти не будет вызван, все cache_hits.
- save_price_rows → status='success', rows_total ≈ 1856.

Если manual-тик после деплоя снова даст ошибку — нужно смотреть
конкретное сообщение (возможен сценарий warehouse'а или Notification —
но эти варианты вне scope текущего фикса).

### 4. Эффективно ли решение, что можно было лучше

**Эффективно** (≈1.5 часа от ошибки скрина до коммита):
- Чёткая декомпозиция: 4 одноразовых diagnostic-скрипта, каждый отвечал
  на свой вопрос — «есть ли вообще наши test-group'ы в prod», «есть ли
  реальные компоненты ПК», «где конкретно они лежат», «насколько чисты
  кандидатные группы».
- Маппинг строился из реальных данных, а не из аналогов / гадания.
- Никаких изменений в логике fetcher'а / runner'а / orchestrator'а —
  меняется только data-constant и тесты. Минимальная поверхность
  изменений = минимальный риск регрессии.

**Чего могло быть лучше:**

1. **Test-стенд РМ должен был содержать prod-подобные group_id'ы.** На
   маркетинговом языке — «нерепрезентативный test environment». Если
   Сергей Волков (контакт РМ) предоставлял test для разработки
   интеграции, то test должен был отражать prod-структуру (хотя бы
   названия групп). Этот урок — на сторону РМ, мы как клиент его
   не контролируем. Но в **наших** будущих интеграциях с поставщиками
   через test-стенды нужно явно валидировать: «работает ли наш
   маппинг на prod-данных» ДО первого тика, а не после.

2. **Bootstrap-каталог 2026-05-12 мог бы дать сигнал.** Мы тогда
   собрали 70 501 row через GetMaterialData без параметров — это в
   2.7× больше test-стенда. Если бы тогда ВНУТРИ bootstrap-скрипта
   был sanity-check «сколько row по каждой из 11 expected group_id»,
   увидели бы 0/0/0/.../0 и подняли бы тревогу за 18 часов до
   scheduled-тика. Урок: при bootstrap-flow на новом стенде
   валидировать ВСЕ ключевые предположения, а не только «catalog
   построился».

3. **UI должен показывать `auto_price_load_runs.status='no_new_data'`
   отдельно от `status='success'`.** Сейчас на скрине журнала
   собственника статус выделен оранжевым бейджем `no_new_data` —
   это хорошо. Но цепочка «тумблер ON → тик прошёл → status='no_new_
   data'» легко может быть прочитана как «всё ок, поставщик просто
   пуст сейчас». Хорошо бы у `no_new_data` была отдельная вкладка/
   фильтр в журнале + по-возможности краткое hint'овое описание (для
   RM: «либо warehouse пуст, либо фильтр group_id не совпадает с
   каталогом поставщика»). Не входит в этот мини-этап.

4. **Заложить smoke-test «маппинг применим к каталогу».** Простой
   periodic-job: `SELECT COUNT(*) FROM resurs_media_catalog WHERE
   material_group = ANY(:expected_groups)`. Если вернёт < N (порог)
   — флажок в `/admin/auto-price-loads` «Внимание: маппинг
   рассинхронен с каталогом поставщика, проверьте
   `_CATEGORY_GROUP_MAP`». Не делал — идея зафиксирована.

### 5. Как было и как стало

**Было после первого мини-этапа Фазы D (10:30 МСК):**
- Тумблер OFF → тик не запускался.
- price_uploads только ручные XLSX от 30.04.2026.
- Action item собственнику: клик в UI.

**Стало после первого мини-этапа Фазы D + действий собственника
(09:45 МСК):**
- Тумблер ON → manual-тик стартанул → status='no_new_data' с пустым
  Material_Tab. Причина — старый маппинг.

**Стало после второго мини-этапа Фазы D (после деплоя этого коммита):**
- `_CATEGORY_GROUP_MAP` адаптирован под prod-каталог РМ (22 group_id
  вместо 11).
- 14 RM-specific тестов passed.
- При следующем manual-тике / scheduled 07:40 МСК завтра — ожидается
  `status='success'` с `rows_total ≈ 1856`.

**Action item собственнику (после деплоя коммита в master через Railway
auto-deploy):**

1. Дождаться deploy'я prod-сервиса (~3-5 минут от push).
2. `/admin/auto-price-loads` → «Запустить сейчас» для «Ресурс Медиа».
3. Через ~30-60 сек проверить журнал — ожидаем `status='success'`,
   `rows_total > 0`.
4. Если снова ошибка — пришли последнюю строку журнала
   (`auto_price_load_runs.error_message`), будем смотреть, что
   именно — возможен сценарий warehouse'а или Notification.

---

## ИТОГ ФАЗЫ D — manual-тик 2026-05-13 10:40 МСК после деплоя

**Победа.** Собственник прислал скриншот журнала запусков с зелёным
бейджем `success` и flash «Загрузка для «resurs_media» запущена.
price_upload_id=48». Sanity-check `scripts/_diag_rm_run48.py` (тоже
одноразовый, не коммитится) подтвердил по prod-БД:

- `price_uploads.id=48`: status='success', uploaded_at=2026-05-13
  10:40:36 МСК, **rows_total=151, rows_matched=151, rows_unmatched=0**.
  `notes`: «updated=150, added=1, skipped=0, errors=0, unmapped(amb=0,
  new=1)» — все 151 SKU нашлись в нашем каталоге по mpn, одна новая
  легла в `unmapped_supplier_items` как кандидат на ручную привязку.
- `auto_price_load_runs.id=43`: started=10:40:32 МСК, finished=10:40:37
  МСК (5 сек), status='success', triggered_by='manual', pu_id=48.
- `auto_price_loads.resurs_media`: enabled=True, status='success',
  last_success_at=2026-05-13 10:40:37 МСК, last_price_upload_id=48.
- `resurs_media_notifications`: **1 row** — Notification API §4.7 тоже
  отработал в том же run'е (runner вызвал его после save_price_rows).
- Разбивка `supplier_prices` для supplier_id=8 по category:
  storage=143, ram=14, psu=13, gpu=11, cooler=10, cpu=7,
  motherboard=1, case=1. Все 8 наших категорий покрыты, хотя по части
  (case, motherboard) только по 1 row — возможно, на сегодня в Москве
  на складе мало корпусов и материнок. Это не наш контроль.

**Прогноз 1856 row не подтвердился — на самом деле 151.** Причина:
fetcher зовёт `GetPrices(WareHouseID="00011", GetAvailableCount=True)`,
сервер РМ возвращает ТОЛЬКО позиции с stock > 0 на этом складе. Полный
каталог в 22 group_id'ах (1856 row) остаётся в `resurs_media_catalog`,
но в `supplier_prices` идут только реально доступные. Это **штатное
поведение** — конфигуратор и должен видеть только то, что можно
действительно купить.

**Что произошло за день 2026-05-13** (сводка для будущей памяти):

| Время МСК | Событие |
|---|---|
| (до 09:00) | scheduled-тик не запустился: `auto_price_loads.enabled=FALSE` |
| ~09:30 | Discovery 1 (1-й чат): диагноз «тумблер OFF». Action item собственнику |
| ~09:40 | Собственник: тумблер ON в UI |
| 09:45:34 | Собственник: «Запустить сейчас». run_id=42, status='no_new_data', пустой Material_Tab (тестовые group_id'ы) |
| ~10:00 | Discovery 2 (этот чат): диагноз «маппинг тестовый ≠ prod-каталог». 4 diagnostic-скрипта по prod-БД |
| ~10:20 | Коммит `d221e64`: переписан `_CATEGORY_GROUP_MAP` на 22 prod-group'а, тесты адаптированы, baseline 2026 passed |
| ~10:30 | Push в master → Railway auto-deploy prod-сервиса |
| 10:40:32 | Собственник: «Запустить сейчас» ещё раз. run_id=43, **status='success', pu_id=48, rows_total=151** |
| (07:40 завтра) | Ожидаем автоматический scheduled-тик — должен пройти аналогично |

**Закрытие:** Фаза D Resurs Media полностью закрыта. Поставщик #5
из 6 в проде работает; остался Green Place (Фаза 12.4 ещё не
закрыта — нет fetcher'а вообще).
